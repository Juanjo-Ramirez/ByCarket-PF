<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contraseña Modificada</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 0;
            background-color: #f5f7fa;
        }
        
        .email-container {
            background-color: white;
            margin: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 12px rgba(16, 54, 99, 0.1);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #103663 0%, #4a77a8 100%);
            color: white;
            padding: 35px 30px;
            text-align: center;
            position: relative;
        }
        
        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="15" cy="25" r="1.5" fill="white" opacity="0.15"/><circle cx="85" cy="30" r="2" fill="white" opacity="0.1"/><circle cx="50" cy="75" r="1" fill="white" opacity="0.15"/><circle cx="25" cy="80" r="1.5" fill="white" opacity="0.1"/></svg>');
            pointer-events: none;
        }
        
        .security-icon {
            font-size: 50px;
            margin-bottom: 15px;
            filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
        }
        
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
            text-shadow: 0 2px 4px rgba(0,0,0,0.2);
        }
        
        .header p {
            margin: 8px 0 0 0;
            font-size: 16px;
            opacity: 0.9;
        }
        
        .content {
            padding: 35px 30px;
        }
        
        .security-box {
            background: linear-gradient(145deg, #e8f8f0 0%, #f0fcf5 100%);
            border-left: 5px solid #27ae60;
            padding: 25px;
            margin: 25px 0;
            border-radius: 0 10px 10px 0;
            position: relative;
            box-shadow: 0 2px 8px rgba(39, 174, 96, 0.08);
        }
        
        .security-box::before {
            content: '🛡️';
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 24px;
            opacity: 0.7;
        }
        
        .security-box p {
            margin: 0 0 15px 0;
            color: #2c3e50;
            font-size: 16px;
        }
        
        .security-box p:last-child {
            margin-bottom: 0;
        }
        
        .security-box .greeting {
            color: #103663;
            font-size: 17px;
            font-weight: 600;
        }
        
        .security-box .confirmation {
            color: #27ae60;
            font-weight: 600;
        }
        
        .warning-box {
            background: linear-gradient(135deg, #fff8e1 0%, #ffeaa7 30%);
            border-left: 5px solid #f39c12;
            padding: 20px;
            margin: 25px 0;
            border-radius: 0 10px 10px 0;
            position: relative;
            box-shadow: 0 2px 8px rgba(243, 156, 18, 0.1);
        }
        
        .warning-box::before {
            content: '⚠️';
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 22px;
        }
        
        .warning-box h4 {
            margin: 0 0 12px 0;
            color: #d68910;
            font-size: 16px;
            font-weight: 600;
        }
        
        .warning-box p {
            margin: 0;
            color: #8b5a00;
            font-weight: 500;
            line-height: 1.5;
        }
        
        .info-section {
            background: linear-gradient(145deg, #f8fbff 0%, #ffffff 100%);
            padding: 20px;
            border-radius: 10px;
            margin: 25px 0;
            border-left: 4px solid #4a77a8;
            box-shadow: 0 2px 8px rgba(16, 54, 99, 0.08);
        }
        
        .security-tips {
            color: #103663;
            font-size: 15px;
        }
        
        .security-tips h4 {
            margin: 0 0 10px 0;
            color: #103663;
            font-weight: 600;
        }
        
        .security-tips ul {
            margin: 10px 0 0 0;
            padding-left: 20px;
            color: #555;
        }
        
        .security-tips li {
            margin-bottom: 5px;
        }
        
        .footer {
            text-align: center;
            padding: 25px 20px;
            background-color: #f8fbff;
            color: #666;
            border-top: 1px solid #eee;
        }
        
        .footer p {
            margin: 8px 0;
            font-size: 15px;
            color: #4a77a8;
            font-weight: 500;
        }
        
        .footer small {
            display: block;
            margin-top: 15px;
            font-size: 13px;
            color: #999;
            font-style: italic;
        }
        
        .date-highlight {
            color: #103663;
            font-weight: 600;
            background: linear-gradient(120deg, rgba(74, 119, 168, 0.1) 0%, rgba(16, 54, 99, 0.1) 100%);
            padding: 2px 6px;
            border-radius: 4px;
        }
        
        @media (max-width: 480px) {
            .email-container {
                margin: 10px;
            }
            
            .content {
                padding: 25px 20px;
            }
            
            .header {
                padding: 30px 20px;
            }
            
            .security-box, .warning-box, .info-section {
                padding: 20px 15px;
            }
            
            .security-box::before, .warning-box::before {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <div class="security-icon">🔐</div>
            <h1>Contraseña Modificada</h1>
            <p>Tu contraseña ha sido actualizada exitosamente</p>
        </div>
        
        <div class="content">
            <div class="security-box">
                <p class="greeting">Hola <strong>{{name}}</strong>,</p>
                <p>Te confirmamos que tu contraseña ha sido modificada exitosamente el <span class="date-highlight">{{date}}</span>.</p>
                <p class="confirmation">✓ Tu cuenta ahora está protegida con la nueva contraseña que configuraste.</p>
            </div>

            <div class="warning-box">
                <h4>Importante - Seguridad de tu Cuenta</h4>
                <p>Si no fuiste tú quien realizó este cambio, por favor contacta con nuestro equipo de soporte inmediatamente.</p>
            </div>
            
            <div class="info-section">
                <div class="security-tips">
                    <h4>💡 Consejos de Seguridad</h4>
                    <ul>
                        <li>Mantén tu contraseña segura y no la compartas</li>
                        <li>Usa contraseñas únicas para cada servicio</li>
                        <li>Considera activar la autenticación de dos factores</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="footer">
            <p>Gracias por mantener tu cuenta segura</p>
            <small>Este es un email automático, por favor no respondas a este mensaje.</small>
        </div>
    </div>
</body>
</html>